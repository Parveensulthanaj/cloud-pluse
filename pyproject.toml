[project]
name = "cloudpulse"
version = "1.0.0"
description = "CloudPulse Autonomous SRE Telemetry & Self-Healing Platform"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
]

[tool.vercel]
entrypoint = "backend.main:app"
